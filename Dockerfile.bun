# Image conteneur sur Bun — sert de point de comparaison de taille/empreinte.
FROM oven/bun:1.3-slim
WORKDIR /app
COPY package.json ./
RUN bun install --production
COPY src ./src
EXPOSE 3000
CMD ["bun", "run", "src/index.ts"]
